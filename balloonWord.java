// class Solution {
// public int maxNumberOfBalloons(String text) {
// Map<Character, Integer> indexMap = new HashMap<>();
// int c = 0;
// for (int i = 0; i < text.length(); i++) {
// if()
// indexMap.put(text.charAt(i), i);
// }
// Map<Character, Integer> indexMapBalloon = new Hashmap<>();

// List<Character> targetLetters = Arrays.asList('b','a','l','l','o','o','n');
// boolean containsAll = indexMap.keySet().containsAll(targetLetters);

// }
// }